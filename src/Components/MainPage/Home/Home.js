import React from 'react';
import Header from '../Header/Header';

const Home = () => {
    return (
        <div className="bg-dark" style={{height: "40rem"}}>
            <Header/>
        </div>
    );
};

export default Home;